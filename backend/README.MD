# BACKEND

First of all do that

```bash
$ cp .env.sample .env
```

Run dev server

```bash
$ npm run dev
```

Run prod server

```bash
$ npm run dev
```

Server will be available by the address localhost:3000
